/** @type {import('next').NextConfig} */
const nextConfig = {
  // Intentionally minimal. No custom chunking config.
  // Build with Turbopack (default): `next build`
  // Build with webpack for comparison: `next build --webpack`
};

module.exports = nextConfig;
